{
  "build": {
    "rootDir": "backend"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && (php artisan migrate --force || true) && php -S 0.0.0.0:8080 -t public public/router.php"
  }
}

